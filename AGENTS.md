# AGENTS.md

本文件用于指导 AI 编码助手在本仓库中工作。除非用户另有明确要求，所有修改都应遵循这里的约定。

## 项目概览

这是一个静态开发者工具站点。根目录是工具入口页，各子目录是独立工具页面，例如：

- `index.html`：工具箱首页
- `json/index.html`：JSON 格式化工具
- `password-generator/index.html`：密码生成器
- `png2ico/index.html`：PNG 转 ICO 工具
- `images/`：站点图片与图标资源

项目不依赖构建流程，页面通常由单个 `index.html` 内联 HTML、CSS 和 JavaScript 组成。

## 工作原则

- 优先保持当前静态站结构，不引入框架、打包器或复杂依赖，除非用户明确要求。
- 修改范围要小，聚焦用户提出的问题，不做无关重构。
- 保留现有页面的中文内容、SEO meta、Open Graph、Twitter Card、结构化数据等信息。
- 新增工具页时，优先沿用现有目录结构：`tool-name/index.html`。
- 面向用户的文案默认使用简体中文。
- 不要提交敏感信息、访问密钥、统计后台配置或本地私有路径。

## HTML/CSS/JavaScript 约定

- 页面必须包含基础可访问性和移动端支持：
  - `lang="zh-CN"`
  - `meta charset="UTF-8"`
  - `meta name="viewport"`
  - 清晰的标题层级
- 样式可以继续内联在页面中，但要保持结构清晰，避免重复的大段无关 CSS。
- JavaScript 默认使用原生浏览器 API，不引入第三方库。
- 工具逻辑应尽量在浏览器本地完成，不上传用户输入的数据。
- 对用户输入要有明确的错误提示，不要让异常直接打断页面交互。
- 复制、下载、转换、清空等操作应给出可见反馈。
- 需要处理文件时，优先使用浏览器标准 API，例如 `FileReader`、`Blob`、`URL.createObjectURL`。

## UI 与体验

- 工具页应以实际功能为首屏重点，不要做营销式落地页。
- 移动端必须可用，常见断点下按钮、输入框、输出区域不能重叠。
- 控件状态要清楚：hover、active、disabled、错误、成功等状态按需提供。
- 保持界面克制、清晰、易扫描；不要加入与工具无关的装饰。
- 对开发者工具类页面，优先保证输入、输出、操作按钮的效率和可读性。

## SEO 与站点信息

- 修改已有页面时，不要随意删除：
  - `<title>`
  - `meta description`
  - `meta keywords`
  - canonical 链接
  - Open Graph / Twitter Card
  - JSON-LD 结构化数据
- 新增页面时，应补齐基本 SEO 信息，并确认 URL 与目录路径一致。
- 如果复制现有页面作为模板，必须更新页面名称、描述、canonical、结构化数据中的 URL 和功能列表。

## 验证要求

完成修改后至少做以下检查：

- 在浏览器中打开相关 HTML 页面，确认页面能正常加载。
- 手动验证核心功能路径，例如输入、转换、复制、清空、下载。
- 检查浏览器控制台是否有明显错误。
- 在窄屏宽度下检查布局是否可用。
- 对纯文本或 JSON 处理工具，使用正常输入和错误输入各验证一次。

如果修改只涉及文档，可以只检查 Markdown 内容是否清晰、无占位符、无明显错别字。

## Git 与文件管理

- 不要覆盖用户未要求修改的文件。
- 不要执行破坏性 Git 命令，例如 `git reset --hard` 或强制 checkout。
- 提交前先查看 `git status`，确认变更范围符合用户请求。
- 保持文件名和目录名语义明确，工具目录推荐使用小写短横线。

## 给 AI 助手的执行建议

在开始编码前，先阅读相关页面的现有实现，理解它的结构和交互方式。实现时优先复用已有风格；完成后用实际页面验证，不只做静态检查。若需求不明确，先给出一个合理默认方案并说明假设，再继续推进。
